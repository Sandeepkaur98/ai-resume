# Pinned for reproducible deploy (Streamlit Cloud, Render, Railway, Heroku) and local dev.
#
# These versions differ slightly from older course handouts (e.g. torch 2.1 / transformers 4.36)
# so that pip can install on Python 3.11–3.13 without building Rust extensions for `tokenizers`.
# PDF reading uses `pypdf` (PyPI name; same role as legacy PyPDF2).

streamlit==1.41.0
sentence-transformers==3.0.1
transformers==4.46.3
torch==2.6.0
numpy==2.2.2
pypdf==4.0.0
